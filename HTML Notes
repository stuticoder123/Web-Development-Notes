Using Paragraph,Heading,Anchor Tags,Image tags with height and width,Boilerplate code,br tag,adding comments(<!-- comments -->)(using by ctrl + forward slash) to print actual heart symbol(&hearts;)(&#9829). 
HTML entities are pieces of text that begin with an ampersand (&) and end with a semicolon (;). They are used to display reserved characters or invisible characters like non-breaking spaces. 
Entities can also be used in place of characters that are difficult to type with a standard keyboard. For example, the entity for the copyright symbol (©) is ©.
Division element to group elements which used to separate the tag which not belong to that particular group which contains all the information in different lines.
Span element works the same as Div element but Span element contains all the information in a single line.
Hr tag(Horizontal rule line element)
Sub and Sup tags for Subscript and Superscript'
Semantic Markup tags(relate to the meaning of the content) :
->meaningful/structured layout
-> SEO friendly
->readable for every users
->improves user experience
To print less than symbol(&lt;),for spaces(&nbsp;),for double inverted commas(&quot;),for ampersand(&amp;),for star(&bigstar;)(unicode of U02605 : &#9733 because 2605 is a decimal number)) Copyright(&copy;),trademark symbol(&reg;)
Emmet Abbreviation used for easy coding :
1.Child : > (nav > ul > li which means li is a child of ul and ul is a child of nav)
2.Sibling : + (p > div + img which means div and img is a child of p tag and div & img are siblings of each other)
3.Multiplication : To multiply one tag many times so that it will be easy for the users to write the code fastly (ul > li*5 which means to write li tag in ul tag 5 times)
making table (<table border = "black">)
4.Item numbering : ul>li.item$*5
<ul>
    <li class="item1"></li>
    <li class="item2"></li>
    <li class="item3"></li>
    <li class="item4"></li>
    <li class="item5"></li>
</ul>
Table has 3 parts :
1.Table header
2.Table body
3.Table footer
Colspan & Rowspan Attributes(Used to create cells which sis occupying over multiple rows or columns)
Forms are used to collect the data from the user.Some terms of form are as follows : 
1.Action Attribute : It is used to define what action needs to be performed when a form is submitted or where the form data should be sent.
<form action = "/action.php">
<form action = "/action">
2.Input element : Used to create multiple form elements .
                  There are multiple types of inputs that can be created using type attribute.(<input/>
3.Placeholder Attribute : <input type = "text" placeholder = "Enter Name">
4.Label element : It represents a caption for an item in a user interface which helps to highlight the box
<label>
Enter your username :
<input type = "text" placeholder = "username">
</label>
-------------------------------------------------------------------------------------------------------------
<label for="username">Enter your username : </label>
<input type = "text" id = "Username" placeholder = "Username" name = "Username"> (where id is a unique name element)
5.Button Element : <button> Submit </button> (It is must to define button element in the form because it is not defined outside the form)
Making a button using input : <input type="submit"> or <input type = "submit" value = "Submit your details">
To search something on Google :  <form action = "https://www.google.com/search">
        <input type = "text" name = "q" placeholder = "Search in Google"/>
        <button>Search</button>
   </form>
Checkbox : <input type = "checkbox" name = "age" id = "age" checked/> (Here checked means that the checkbox is already ticked.) after which age = on is passed in the final result link.
           <label for = "age"> I am 18+ </label>
Radio button : <input type = "radio" name = "fruit" id = "apple" "value = "apple"/> (here url will become age = on & fruit = mango)
                <label for = "apple">Apple </label> (only 1 radio button is filled if name attribute of each radio button is same) 
6.Select element : To make a dropdown button
<div>
Choose your profession :
<select name ="profession" id = "Profession">
  <option> Select your option </option>
  <option value = "student">Student </option>
  <option value = "dev">Developer </option>
</select>
7.Range element : step for step wise increment and value attribute for pre - assigning it to the exact value
</div>  (Profession = dev) try to write value = "dev" selected > for pre-selection
8.Text area for message box
